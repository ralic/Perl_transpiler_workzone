#!/usr/bin/perl -w

$x = 1;
while ($x <= 10) {
    push ( @a, 5 );
	pop @a;
shift (@a );
 unshift(@myNames, 'Larry'); 
reverse(@myNames); 

	@array;
	$a = 4;
	@array   = ();

	%array  ;
	%array   = ()  ;

	while   (  $a  <   5  )    {

    print "$x\n";
    $x = $x + 1;
}
